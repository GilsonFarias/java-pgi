package com.company.pgi.controller.permissions;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/permissions")
public class PermissionsController {
    // @Autowired
    // private IPermissionsService iPermissionsService;

    // @GetMapping("/{profile_id}")
    // public List<Permissions> getPermissionsSP(@PathVariable Long profile_id) {
    //     List<Permissions> permission = iPermissionsService.user_permissions(profile_id);
    //     return permission;
    // }

}
